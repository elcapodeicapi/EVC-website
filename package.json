{
  "name": "root",
  "private": true,
  "dependencies": {
    "bcrypt": "^6.0.0",
    "functions": "file:Backend/functions",
    "jsonwebtoken": "^9.0.2",
    "path-match": "^1.2.4",
    "root": "file:"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "scripts": {
    "start": "concurrently -n API,WEB -c green,cyan \"node Backend/server.js\" \"npm --prefix cliet run dev\"",
    "start:api": "node Backend/server.js",
    "start:web": "npm --prefix cliet run dev",
    "lint": "npm --prefix Backend/functions run lint",
    "build": "npm run lint && npm --prefix cliet run build"
  }
}
